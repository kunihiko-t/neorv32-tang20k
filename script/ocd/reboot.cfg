# CPUを再起動し、ブートローダーにIMEMイメージを自動起動させる。
# `reg pc`のCSR経路は不安定なため使わない。
set HERE [file dirname [file normalize [info script]]]
set REPO [file normalize [file join $HERE .. ..]]
source [file join $REPO script ocd target.cfg]
target_setup 1
reset run
shutdown
